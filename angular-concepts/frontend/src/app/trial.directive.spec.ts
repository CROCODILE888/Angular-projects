import { TrialDirective } from './trial.directive';

describe('TrialDirective', () => {
  it('should create an instance', () => {
    const directive = new TrialDirective();
    expect(directive).toBeTruthy();
  });
});
